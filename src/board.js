// knows the state of fields of the board: if they are claimed, and then in what way

(function(exports){
  function Board(){
    this.fields = [Array[3], Array[3], Array[3]];
  };

  exports.Board = Board;
})(this);
